apply plugin: "war"

bootWar {
    mainClassName = "de.fibz.beyond.unit.test.TestcontainerSampleProjectApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    webAppDirName = "build/resources/main/static/"
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]
}
